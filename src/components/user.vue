<template>
  <div class="my-page">
    <div class="user-info">
      <van-image class="avatar" round :src="avatarUrl" />
      <div class="username">{{ username }}</div>
    </div>
    <van-cell-group>
      <van-cell icon="info-o" title="我的订单" is-link @click="order" />
      <van-cell icon="info-o" title="我的地址" is-link @click="address" />
      <van-cell icon="logout" title="退出登录" is-link @click="logout" />
    </van-cell-group>
    <HomeTabber></HomeTabber>
  </div>
</template>

<script setup>
import { useRouter } from 'vue-router'
import HomeTabber from './HomeTabber.vue';

let avatarUrl = 'https://ui-avatars.com/api/?name=John+Doe';
let username = 'John Doe';
const route = useRouter();

function address() {
  route.push("/addresslist");
}

function order() {
  route.push("/orderlist");
}
function logout(){
  route.push("/logout")
}
</script>

<style scoped>
.my-page {
  max-width: 400px; 
  margin: 0 auto; 
  padding: 16px;
}

.user-info {
  display: flex;
  align-items: center;
  padding: 16px 0;
}

.avatar {
  width: 60px;
  height: 60px;
  margin-right: 16px;
}

.username {
  font-size: 18px;
  font-weight: bold;
}
</style>
